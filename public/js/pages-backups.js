'use strict';window.Pages=window.Pages||{};Core.route('my-backup',async()=>{document.getElementById('content').innerHTML=`${Core.pageHead('My Data Backup','Download your own permitted tasks and file list')}<div class="card card-pad"><h3>Personal backup</h3><p class="muted">Exports only your own permitted tasks and documents. Company financial data is never included.</p><button class="btn btn-gold" onclick="Pages.downloadPersonalBackup()">Download my backup</button></div>${['admin','super_admin'].includes(Core.state.user.role)?'<div class="card card-pad" style="margin-top:16px"><h3>Company restore request</h3><p class="muted">Submit a request for Super Admin review. No data is restored automatically.</p><button class="btn btn-outline" onclick="Pages.requestRestore()">Request company restore</button></div>':''}`;});Pages.downloadPersonalBackup=async()=>{const r=await fetch('/api/backups/my-data',{credentials:'same-origin'});const b=await r.blob();const a=document.createElement('a');a.href=URL.createObjectURL(b);a.download='my-tech-defenders-backup.json';a.click();URL.revokeObjectURL(a.href);};Pages.requestRestore=()=>Core.formModal({title:'Request company restore',fields:[{name:'reason',label:'Reason / backup reference *',type:'textarea',required:true}],submitLabel:'Submit for approval',onSubmit:async v=>{await Core.post('/backups/company-restore-requests',v);toast('Requested','Super Admin approval is required','success');}});
