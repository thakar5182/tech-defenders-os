'use strict';Core.route('my-plan',async()=>{const d=await Core.get('/subscription/current'),s=d.subscription,end=s.status==='trial'?s.trialEndsAt:s.renewalDate;document.getElementById('content').innerHTML=`${Core.pageHead('My Plan','Your workspace subscription details')}<div class="kpi-grid">${Core.kpi('Plan',s.plan,'₹'+s.amount+'/month')}${Core.kpi('Status',s.status,s.status==='trial'?'Trial ends '+Core.fmtDate(end):'Renewal '+Core.fmtDate(end))}${Core.kpi('Users',d.users+' / '+s.userLimit,'Active users')}</div>`;});
