'use strict';window.Pages=window.Pages||{};async function salesCustomers(){return (await Core.get('/crm/customers')).customers;}
Core.route('sales/visits',async()=>{const[d,c]=await Promise.all([Core.get('/sales-operations/visits'),salesCustomers()]);Pages._sc=c;document.getElementById('content').innerHTML=`${Core.pageHead('Sales Visits','Plan and record customer visits',Core.can('sales','create')?'<button class="btn btn-gold" onclick="Pages.visitForm()">+ New visit</button>':'')}${Core.table([{label:'Visit',key:'number'},{label:'Customer',key:'customerName'},{label:'Date',render:r=>Core.fmtDate(r.date)},{label:'Purpose',key:'purpose'},{label:'Outcome',key:'outcome'}],d.visits,{emptyTitle:'No sales visits',emptyText:'Plan your first customer visit.'})}`;});
Pages.visitForm=()=>Core.formModal({title:'Sales visit',fields:[{name:'customerId',label:'Customer *',type:'select',required:true,options:(Pages._sc||[]).map(c=>({value:c.id,label:c.name}))},{name:'date',label:'Date',type:'date',value:new Date().toISOString().slice(0,10),half:true},{name:'purpose',label:'Purpose *',required:true,half:true},{name:'outcome',label:'Notes / outcome',type:'textarea'}],submitLabel:'Save visit',onSubmit:async v=>{await Core.post('/sales-operations/visits',v);toast('Saved','Sales visit added','success');Core.render();}});
Core.route('sales/dispatches',async()=>{const[d,c]=await Promise.all([Core.get('/sales-operations/dispatches'),salesCustomers()]);Pages._sc=c;document.getElementById('content').innerHTML=`${Core.pageHead('Dispatch', 'Track goods leaving for customers',Core.can('sales','edit')?'<button class="btn btn-gold" onclick="Pages.dispatchForm()">+ New dispatch</button>':'')}${Core.table([{label:'Dispatch',key:'number'},{label:'Customer',key:'customerName'},{label:'Reference',key:'reference'},{label:'Transporter',key:'transporter'},{label:'Vehicle',key:'vehicleNo'},{label:'Date',render:r=>Core.fmtDate(r.dispatchDate)}],d.dispatches,{emptyTitle:'No dispatches',emptyText:'Create a dispatch record after delivery preparation.'})}`;});
Pages.dispatchForm=()=>Core.formModal({title:'New dispatch',fields:[{name:'customerId',label:'Customer *',type:'select',required:true,options:(Pages._sc||[]).map(c=>({value:c.id,label:c.name}))},{name:'reference',label:'Order / invoice reference *',required:true},{name:'transporter',label:'Transporter',half:true},{name:'vehicleNo',label:'Vehicle no.',half:true},{name:'dispatchDate',label:'Dispatch date',type:'date',value:new Date().toISOString().slice(0,10)}],submitLabel:'Create dispatch',onSubmit:async v=>{await Core.post('/sales-operations/dispatches',v);toast('Saved','Dispatch created','success');Core.render();}});
Core.route('sales/gate-passes',async()=>{const d=await Core.get('/sales-operations/gate-passes');document.getElementById('content').innerHTML=`${Core.pageHead('Gate Passes','Control material moving in and out',Core.can('sales','edit')?'<button class="btn btn-gold" onclick="Pages.passForm()">+ New gate pass</button>':'')}${Core.table([{label:'Pass',key:'number'},{label:'Party',key:'party'},{label:'Direction',render:r=>Core.badge(r.direction)},{label:'Purpose',key:'purpose'},{label:'Vehicle',key:'vehicleNo'},{label:'Date',render:r=>Core.fmtDate(r.date)}],d.gatePasses,{emptyTitle:'No gate passes',emptyText:'Record incoming or outgoing material movement.'})}`;});
Pages.passForm=()=>Core.formModal({title:'Gate pass',fields:[{name:'party',label:'Party *',required:true},{name:'purpose',label:'Purpose *',required:true},{name:'direction',label:'Direction',type:'select',half:true,options:[{value:'out',label:'Outward'},{value:'in',label:'Inward'}]},{name:'vehicleNo',label:'Vehicle no.',half:true},{name:'date',label:'Date',type:'date',value:new Date().toISOString().slice(0,10)}],submitLabel:'Create pass',onSubmit:async v=>{await Core.post('/sales-operations/gate-passes',v);toast('Saved','Gate pass created','success');Core.render();}});
