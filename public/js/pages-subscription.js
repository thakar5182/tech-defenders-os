'use strict';window.Pages=window.Pages||{};Core.route('admin/subscription',async()=>{const d=await Core.get('/subscription/current');const s=d.subscription;const end=s.status==='trial'?s.trialEndsAt:s.renewalDate;document.getElementById('content').innerHTML=`${Core.pageHead('Plan & Subscription','Control this workspace plan, trial and user access',Core.can('admin','edit')?'<button class="btn btn-gold" onclick="Pages.subscriptionForm()">Manage plan</button>':'')}<div class="kpi-grid">${Core.kpi('Plan',s.plan,'₹'+s.amount+'/month')}${Core.kpi('Status',s.status,s.status==='trial'?'Trial ends '+Core.fmtDate(end):'Renewal '+Core.fmtDate(end))}${Core.kpi('Users',d.users+' / '+s.userLimit,'Active workspace users')}</div><section class="section-block"><h3>Commercial settings</h3><div class="meta-grid"><div class="meta-item"><span>Started</span><b>${Core.fmtDate(s.startedAt)}</b></div><div class="meta-item"><span>Next date</span><b>${Core.fmtDate(end)}</b></div><div class="meta-item"><span>Billing</span><b>₹${s.amount} / month</b></div></div></section>`;Pages._subscription=s;});Pages.subscriptionForm=()=>{const s=Pages._subscription;Core.formModal({title:'Manage subscription',fields:[{name:'plan',label:'Plan',value:s.plan},{name:'status',label:'Status',type:'select',options:['trial','active','past_due','suspended','cancelled'].map(x=>({value:x,label:x})),value:s.status},{name:'userLimit',label:'User limit',type:'number',half:true,value:s.userLimit},{name:'amount',label:'Monthly amount',type:'number',half:true,value:s.amount},{name:'trialEndsAt',label:'Trial end',type:'date',half:true,value:String(s.trialEndsAt||'').slice(0,10)},{name:'renewalDate',label:'Renewal date',type:'date',half:true,value:String(s.renewalDate||'').slice(0,10)}],submitLabel:'Save plan',onSubmit:async v=>{await Core.patch('/subscription/current',v);toast('Saved','Subscription updated','success');Core.render();}});};
